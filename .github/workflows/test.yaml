name: Test
on: [ pull_request ]

jobs:
  linux_build:
    name: Linux Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Container
        run: |
          docker buildx build -t devcontainer --platform=linux/amd64 .
      - name: Build Kernel
        run: |
          docker run \
            --mount type=bind,source="$(realpath "$(pwd)")",target="/tensorflow_nearest_neighbours" \
            --platform=linux/amd64 --rm devcontainer tox run