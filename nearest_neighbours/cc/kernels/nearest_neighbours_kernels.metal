#include <metal_stdlib>using namespace metal;// The hash encode forward kernel function.kernel void HashEncodeForward(    // inputs    const device float* embeddings_batch [[buffer(0)]],    const device float* embedding_matrix [[buffer(1)]],    // output    device float* outputs [[buffer(3)]],    // attributes    constant int& batch_size [[buffer(4)]],    constant int& sequence_length [[buffer(5)]],    constant int& vocab_size [[buffer(6)]],    // thread id    uint3 tid [[thread_position_in_grid]]) {}